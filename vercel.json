{
  "rootDirectory": "girlfriends-day",
  "buildCommand": "npm install --include=dev && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
